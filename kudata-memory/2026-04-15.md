# Git 变更回顾 - 2026-04-15

## 概览

分支：`dev`（相对当前 `HEAD` 的**工作区未提交变更**）  
修改文件总数：7 个已跟踪文件 + 1 个新增未跟踪组件  
已跟踪 diff 统计：约 +88 / -5 行；新增 `excel-viewer.tsx` 约 182 行

---

## 修改的文件详情

### Dockerfile

- `apt-get install` 增加 `nodejs`、`npm`（与原有 `libgcc-s1`、`libstdc++6`、`ripgrep`、`curl`、`git` 同条安装）
- 原注释掉的 `RUN nodejs npm && npm config set registry ...` 改为**启用**单行：`RUN npm config set registry http://di-mirrors.tmeoa.com/repository/npm-public/`

---

### packages/app/package.json / bun.lock

- 新增依赖：`xlsx` `0.18.5`（`bun.lock` 同步）
- `monaco-editor` 在 `dependencies` 内仅做**排序位置调整**（无版本变更）

---

### packages/opencode/src/file/index.ts

**电子表格按二进制读取并 base64 返回**

- 新增扩展名集合 `spreadsheet`：`xls`、`xlsx`、`xlsm`、`xlsb`，及判断 `isSpreadsheet`
- 在 `read` 流程中：若命中 `isSpreadsheet`，存在则 `readFile` → `Buffer.from(bytes).toString("base64")`，返回 `type: "binary"`、`encoding: "base64"`、`mimeType: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"`；不存在或读失败则返回空二进制占位

---

### packages/app/src/components/excel-viewer.tsx（新增，未跟踪）

**基于 `xlsx` 的表格预览**

- `parse(base64)`：`atob` → `Uint8Array` → `read(bytes, { type: "array" })`，失败返回 `undefined`
- `sheetToRows`：`utils.sheet_to_json(..., { header: 1, defval: "" })`
- UI：`useTheme` / `useLanguage`；多 sheet 顶部 Tab；主区域 HTML `table`（首列行号 `#`，表头为 A/B/C… `colLabel`）；空表 `excel.empty`；解析失败 `excel.error`；底部统计行/列/sheet 数量（`excel.stats.*`）
- 样式：`border-border-weak-base`、`bg-surface-base` / `bg-surface-raised-base`、`text-13-regular` 等现有 token

---

### packages/app/src/pages/session/file-tabs.tsx

**会话内文件 Tab：Markdown 编辑/预览 + Excel 视图**

- 引入 `createSignal`、`Show`、`Markdown`、`ExcelViewer`（另新增 `Icon` 的 import，当前文件内未见对 `Icon` 组件的直接使用，若触发 unused 可后续删掉该 import）
- `isExcel`：`path` 匹配 `/\.(xlsx?|xlsm|xlsb|csv)$/i`
- `isMd`：`/\.md$/i`
- `preview` 信号；`createEffect(on(path, () => setPreview(false)))` 切换文件时重置为编辑模式
- Markdown：右上角分段按钮「编辑 / 预览」（`editor.mode.editor` / `editor.mode.preview`），预览分支用 `ScrollView` + `Markdown`，`cacheKey` 沿用原逻辑
- `Switch` 中新增 `<Match>`：已加载且 `isExcel()` 时渲染 `<ExcelViewer content={contents()} filePath={...} />`；已加载且 Markdown 且 `preview()` 时走 Markdown 分支（先于原 `MonacoEditor` 媒体判断顺序需以代码为准）

---

### packages/app/src/i18n/en.ts / zh.ts

| key | en | zh |
|-----|----|----|
| `editor.mode.editor` | Editor | 编辑 |
| `editor.mode.preview` | Preview | 预览 |
| `excel.error` | Failed to parse Excel file | Excel 文件解析失败 |
| `excel.empty` | This sheet is empty | 此工作表为空 |
| `excel.stats.rows` | `{{count}} rows` | `{{count}} 行` |
| `excel.stats.cols` | `{{count}} columns` | `{{count}} 列` |
| `excel.stats.sheets` | `{{count}} sheets` | `{{count}} 个工作表` |

- `en.ts`：`workspace.reset.note` 后多余空行删除（纯格式）

---

## 本次变更的主题

1. **Excel / 表格预览** — 后端对 `xls`/`xlsx`/`xlsm`/`xlsb` 以 base64 二进制下发；前端 `ExcelViewer` 用 `xlsx` 解析并表格展示，多 sheet 切换与行列统计
2. **Markdown 双模式** — 同一会话文件 Tab 内「编辑」与「预览」切换，预览走 `Markdown` 组件
3. **Docker 前端构建链** — 镜像内安装 Node/npm 并配置内网 npm registry，便于容器内安装或构建依赖
4. **依赖** — `xlsx@0.18.5` 写入 app 包与 lockfile

---

## 说明

- 上述内容为 **2026-04-15** 根据当前仓库工作区与 `HEAD` 的差异整理。
- `excel-viewer.tsx` 在 `git status` 中为 **Untracked**，已纳入本记忆文件描述。
- 前端 `isExcel` 含 **`.csv`**，后端 `spreadsheet` 集合未包含 `csv`：`.csv` 仍可能走原有文本读取路径，与 Excel 分支是否一致需联调时注意。
