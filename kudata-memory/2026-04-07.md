# Git 变更回顾 - 2026-04-07

## 概览

分支：`chore/sync-upstream-v1.3.13`  
修改文件总数：20 个文件  
总变更：新增 264 行，删除 116 行

---

## 修改的文件详情

### DEVELOPMENT.md

- 新增版本标记：`opencode-v125-03282001 45f94d09e8a3`
- 修正前端构建命令：改为 `bun run --cwd packages/app build`，添加 `OPENCODE_BASE_PATH` 导出
- 后端构建：新增 `OPENCODE_CHANNEL=latest OPENCODE_VERSION=1.3.13 ./packages/opencode/script/build.ts`
- Docker 镜像名更新为 `opencode-v1313-04052340`
- docker run 端口改为 `-p 4097:4096`
- 新增挂载目录 `base-1333/config` 和 `base-1333/local`
- 新增环境变量 `-e OPENCODE_GLOBAL_PLUGIN_INSTALL_ONLY=true`
- 记录服务地址：`10.34.81.146:4097`

---

### packages/app/src/components/dialog-scheduler.tsx

- 默认模型 `DEFAULT_MODEL` 从 `Gemini-3.1-Pro` 改为 `Claude-Sonnet-4.6`

---

### packages/app/src/components/dialog-select-model.tsx

- 模型列表过滤：新增过滤掉 `provider.id === "opencode"` 的内置模型，不在选择列表中显示

---

### packages/app/src/components/dialog-select-skill.tsx

**重构：技能选择支持多选**

- `SkillList` 组件新增 `selected: () => string[]` prop，`onSelect` 改为 `onToggle`
- 列表项 UI 新增 checkbox 样式（勾选图标），已选中技能高亮显示
- `SkillSelectorPopover` 的 `onSelect` 改为 `onToggle`，新增 `selectedSkills` prop
- 选择后不再自动关闭弹窗（支持多选持续选择）

---

### packages/app/src/components/file-tree.tsx

- `InlineInput` 组件添加 `done` 防重复提交标志，避免 `onConfirm`/`onCancel` 被重复调用

---

### packages/app/src/components/prompt-input.tsx

**重构：技能显示支持多选**

- 技能标签区 `Show` 条件从 `local.skill.current()` 改为 `local.skill.current().length > 0`
- 使用 `<For>` 循环渲染多个技能 tag，支持同时显示多个已选技能
- 每个技能 tag 的删除按钮改为调用 `local.skill.toggle(name)`

---

### packages/app/src/components/prompt-input/submit.ts

**重构：支持多技能提交**

- 提交后清空技能：`local.skill.set(undefined)` 改为 `local.skill.set([])`
- 提示词拼接：从单技能 `` 使用`${skill}` 技能 `` 改为多技能格式 `` 使用 `技能A`、`技能B` 技能回答问题 ``

---

### packages/app/src/components/session/session-new-view.tsx

- `projectRoot` 计算从 `sync.project?.worktree ?? sdk.directory` 简化为直接使用 `sdk.directory`
- 添加 `console.log("isWorktree", isWorktree)` 调试日志（临时）

---

### packages/app/src/components/settings-token-management.tsx

**更新默认配置模板**

- provider npm 包从 `@ai-sdk/openai-compatible` 改为 `@ai-sdk/github-copilot`
- provider id 保持 `tme-continue-provider`，model 指向 `tme-continue-provider/GPT-5.4`
- 模型列表调整：移除 `GLM-4.7`、`GPT-5-mini`、`GPT-5.1`
- 新增模型：`GPT-5.4-mini`、`GPT-5.4-nano`、`TME DeepSeek-V3.1-Terminus`、`TME DeepSeek-V3.2`
- modalities 格式改为多行 JSON 格式（代码格式化）

---

### packages/app/src/context/file.tsx

**修复文件写入后编辑器内容同步 & URL 构建问题**

- `writeFile` 写入后立即更新编辑器缓存（`setLoaded`），避免下次打开仍显示旧内容
- `downloadFile` 和 `serveUrl` 中 URL 构建方式修复：从 `new URL("/file/download", sdk.url)` 改为 `` new URL(`${sdk.url.replace(/\/$/, "")}/file/download`) ``，避免 `sdk.url` 含 base path 时路径被截断

---

### packages/app/src/context/local.tsx

**重构：skill 状态支持多选**

- `skill` store 类型从 `{ current?: string }` 改为 `{ current: string[] }`，初始值为 `[]`
- `current()` 返回 `string[]`
- `set(names: string[])` 替换整个列表
- 新增 `toggle(name: string)` 方法：已选则移除，未选则添加

---

### packages/app/src/entry.tsx

**修复：支持 base path 部署时 API URL 构建**

- 新增 `apiPath()` 函数，从 `import.meta.env.BASE_URL` 中提取路径前缀（去除末尾斜杠）
- `getCurrentUrl()` 在非 DEV 模式下将 `origin` + `prefix` 组合为完整 API URL
- 解决 opencode 部署在子路径（如 `/kgbi/starbot`）时 WebSocket/API 请求 URL 不正确的问题

---

### packages/app/src/i18n/en.ts / zh.ts

新增两条翻译 key：
- `fileTree.upload.start`：`"Upload started"` / `"开始上传"`
- `fileTree.toast.deleteSuccess`：`"Deleted"` / `"删除成功"`

---

### packages/app/src/pages/directory-layout.tsx

- 新增 `base` 常量（`import.meta.env.BASE_URL` 去末尾斜杠）
- `onSessionHref` 返回值前缀加入 `base`，修复子路径部署下会话链接跳转不正确的问题

---

### packages/app/src/pages/session/session-side-panel.tsx

**改进文件上传/删除的 toast 提示交互**

- 上传流程：新增"开始上传" toast → 显示持久 loading toast → 上传完成/失败后 dismiss loading
- 删除文件：成功后新增 `deleteSuccess` toast
- 批量删除：成功后同样显示 `deleteSuccess` toast
- 使用 `toaster.dismiss(tid)` 主动关闭 loading toast
- 错误处理逻辑由 `.catch()` 链改为捕获变量再判断，避免 `setUploading(false)` 顺序问题

---

### packages/opencode/src/config/config.ts

**新增环境变量 `OPENCODE_GLOBAL_PLUGIN_INSTALL_ONLY` 支持**

- 当该 flag 为 `true` 时，插件依赖安装仅在全局 config 目录进行（`~/.config/opencode` 或 `Flag.OPENCODE_CONFIG_DIR`），跳过 home 级和项目级目录
- 修改逻辑：将 `installDependencies` 调用包裹在 `if (!Flag.OPENCODE_GLOBAL_PLUGIN_INSTALL_ONLY || isGlobal)` 判断中

---

### packages/opencode/src/flag/flag.ts

- 新增 `OPENCODE_GLOBAL_PLUGIN_INSTALL_ONLY` flag 声明和动态 getter（`Object.defineProperty`，运行时读取环境变量）
- 与已有的 `OPENCODE_DISABLE_PROJECT_CONFIG` 模式一致，注释说明其作用

---

### packages/opencode/src/provider/sdk/copilot/chat/openai-compatible-chat-language-model.ts

- 删除 3 处调试 `console.log`：constructor、doStream 入口、finish_reason 输出（清理遗留日志）

---

### packages/opencode/src/server/server.ts

- skill push 返回 URL 修改：从拼接 GitLab 分支 URL 改为返回 `https://kudata-agent.tmeoa.com/skill-market/${repoName}` 市场 URL
- 新增 `repoName` 变量（从 remote URL 解析仓库名）
- 新增导出 `export const Default = App`

---

## 本次变更的主题

1. **多技能选择** — skill 从单选改为多选，UI/状态/提交全链路更新
2. **Base Path 部署修复** — API URL、session href 均正确带上子路径前缀
3. **文件写入同步** — writeFile 后立即同步编辑器内容
4. **上传/删除 Toast 优化** — loading 状态、成功/失败提示更完善
5. **新 Flag：`OPENCODE_GLOBAL_PLUGIN_INSTALL_ONLY`** — 多租户部署下仅安装全局插件依赖
6. **模型配置更新** — 默认模型、provider npm 包、可用模型列表均随上游版本同步
7. **清理调试日志** — 移除 openai-compatible 模型中的临时 console.log
