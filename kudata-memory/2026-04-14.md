# Git 变更回顾 - 2026-04-14

## 概览

分支：`dev`（相对当前 `HEAD` 的**工作区未提交变更**）  
修改文件总数：18 个已跟踪文件  
总变更：新增约 1412 行，删除约 603 行（大部分为 SDK 自动生成文件）

---

## 修改的文件详情

### DEVELOPMENT.md

- 新增版本记录：`opencode-v1313-04101052`
- Docker 镜像标签更新：`opencode-v1313-04101052` → `opencode-v1313-04142202`
- `docker build/save/load/run` 命令中镜像名同步更新
- `docker run` 新增环境变量：`-e OPENCODE_WEBFETCH_PROXY=http://10.5.135.172:2443`

---

### packages/opencode/src/flag/flag.ts

- 新增环境变量 Flag：`OPENCODE_WEBFETCH_PROXY`，读取 `process.env["OPENCODE_WEBFETCH_PROXY"]`，专供 WebFetch 工具使用

---

### packages/opencode/src/tool/webfetch.ts

**代理支持**

- 文件顶部读取 `WEBFETCH_PROXY`（来自环境变量 `OPENCODE_WEBFETCH_PROXY`）
- `parameters` 新增可选字段 `proxy: z.string()`，允许调用方传入代理 URL
- `execute` 传给日志的 input 中同步携带 `proxy`
- fetch 请求（初次 + Cloudflare 重试）均通过 `fetchOpts.proxy` / `retryOpts.proxy` 注入代理；参数优先于环境变量

---

### packages/opencode/src/file/index.ts

- 删除 `log.info("init")` 冗余日志行
- 新增 `File.share(filepath)` 异步函数：
  - 路径安全校验（`containsPath`，防止目录穿越）
  - 通过 `FormData` 上传文件到 `http://10.5.132.186:8000/api/v1/report-html/upload`
  - 解析返回 `json.url` 或 `json.data.url`
  - 相对路径（`/` 开头）自动补全为 `https://kudata-agent.tmeoa.com{path}`
  - 返回最终可分享 URL

---

### packages/opencode/src/server/routes/file.ts

- 新增路由 `GET /file/share`：
  - query 参数：`path: z.string()`
  - 调用 `File.share()` 并返回 `{ url: string }`
  - operationId: `file.share`

---

### packages/app/src/context/file.tsx

- 新增 `share(target)` 方法：向 `/file/share?path=...&directory=...` 发 GET 请求，返回 `{ url: string }`
- 将 `share` 暴露到 `useFile()` context

---

### packages/app/src/components/file-tree.tsx

**右键菜单新增两项操作**

- `FileTreeOps` 新增可选回调：`onCopy?(node)` 和 `onShare?(node)`
- 菜单项「复制」（`icon: copy`）插入到「下载」之后
- 菜单项「生成共享链接」（`icon: share`）追加到菜单末尾
- `ContextMenu.Separator` 分隔线移至「删除」之后（原在 HTML 预览前），视觉分组更合理

---

### packages/app/src/pages/session/session-side-panel.tsx

**实现 onCopy 与 onShare 两个回调**

- `onCopy`：fetch 文件内容 → 在原文件名加 `_copy` 后缀（保留扩展名）→ `file.write()` 写入 → toast 成功/失败
- `onShare`：调用 `file.share()` 获取 URL → `navigator.clipboard.writeText()` 复制到剪贴板 → toast 成功/失败

---

### packages/app/src/i18n/en.ts / zh.ts

新增文案（`fileTree` 命名空间）：

| key | en | zh |
|-----|----|----|
| `fileTree.menu.copy` | Copy file | 复制 |
| `fileTree.menu.share` | Generate share link | 生成共享链接 |
| `fileTree.toast.copyFailed` | Failed to copy file | 复制文件失败 |
| `fileTree.toast.copySuccess` | File copied | 文件已复制 |
| `fileTree.toast.shareFailed` | Failed to generate share link | 生成共享链接失败 |
| `fileTree.toast.shareSuccess` | Share link copied to clipboard | 共享链接已复制到剪贴板 |

zh.ts 中 `fileTree.menu.preview` 由「在浏览器中预览」缩短为「浏览器预览」

---

### packages/app/src/components/prompt-input/slash-popover.tsx

**新增 `/` 命令弹窗面板**

- 新增导出类型 `SlashCommand`：`{ id, trigger, title, description?, keybind?, type, source? }`
- `PromptPopoverProps` 扩展：`popover` 支持 `"slash"`，新增 `slashFlat/slashActive/setSlashActive/onSlashSelect/commandKeybind/setSlashPopoverRef`
- 弹窗由单一 `<Show when="at">` 改为 `<Switch><Match when="at">/<Match when="slash">` 双分支
- slash 分支：`<For each={slashFlat}>` 渲染命令列表，每项含 `data-slash-id` 属性（供键盘导航 scrollIntoView）、来源徽标（skill / mcp / custom）、keybind 提示
- 空结果 fallback：`prompt.popover.emptyCommands` i18n key

---

### packages/app/src/components/prompt-input.tsx

**`/` 命令弹窗联动**

- `store.popover` 类型由 `"at" | null` 扩展为 `"at" | "slash" | null`
- 新增 `ALLOWED_SLASH` 白名单 Set：`new/undo/redo/compact/fork/open/model`
- `slashCommands`：从 `command.options` 过滤出 `slash` 在白名单内且未禁用的命令，映射为 `SlashCommand[]`
- `handleSlashSelect`：清空编辑器 → `command.trigger(cmd.id, "slash")`
- `useFilteredList<SlashCommand>` 实例：`slashFlat/slashActive/setSlashActive/slashOnInput/slashOnKeyDown`
- `createEffect`：`slashActive()` 变化时 `requestAnimationFrame` 中用 `scrollIntoView` 滚动到激活项
- 输入检测：原文为 `/xxx`（`^\/(\S*)$`）时触发 slash 弹窗；否则走原逻辑
- `Enter` 确认：`popover === "slash"` 时调用 `handleSlashSelect`
- `ArrowUp/Down`：`popover === "slash"` 时路由到 `slashOnKeyDown`
- `promptProbe.set` 增加 `slash: { active, ids }`（测试探针）

---

### packages/app/src/pages/session/use-session-commands.tsx

**精简 slash 触发器**

- 移除以下命令的 `slash` 字段（不再作为 slash 命令暴露）：
  - `session.share`（`slash: "share"`）
  - `session.unshare`（`slash: "unshare"`）
  - `terminal.toggle`（`slash: "terminal"`）
  - `mcp.toggle`（`slash: "mcp"`）
  - `agent.cycle`（`slash: "agent"`）

---

### packages/app/src/pages/layout.tsx

- 移除 `workspace.toggle` 命令的 `slash: "workspace"` 字段

---

### packages/app/src/testing/prompt.ts

- `PromptProbeState.popover` 类型扩展为 `"at" | "slash" | null`
- 新增可选字段 `slash?: { active: string | null; ids: string[] }` 供 E2E 测试读取

---

### packages/app/src/components/settings-token-management.tsx

**为所有模型补充 `limit` 字段（context/output token 上限）**

| 模型 | context | output |
|------|---------|--------|
| Claude-Haiku-4.5 | 100000 | 64000 |
| Claude-Sonnet-4.5/4.6 | 100000 | 64000 |
| GPT-5-mini | 200000 | 64000 |
| GPT-5.1 / 5.2 / 5.4-nano | 200000 | 64000 |
| GPT-5.4 | 525000 | 64000 |
| Gemini-3-Flash | 100000 | 64000 |
| Gemini-3.1-Pro | 500000 | 64000 |
| TME DeepSeek-V3.1/V3.2 | 64000 | 32000 |
| DeepSeek-V3.2 (conv2) | 64000 | 32000 |
| GLM-4.7 / 5.1 / 5 | 100000 | 64000 |
| Kimi-K2.5 | 128000 | 64000 |

---

### packages/sdk/js/src/v2/gen/sdk.gen.ts / types.gen.ts

- 自动生成文件，由 SDK 构建脚本重新生成
- 包含 `/file/share` 新端点的类型定义及 webfetch `proxy` 参数的类型变更
- 变更量大（+1048/-603 行），属机械性 codegen 产物

---

## 本次变更的主题

1. **文件共享链接** — 后端 `File.share()` 上传到报告服务器取 URL，路由 `/file/share`，前端 context `share()`，文件树右键菜单「生成共享链接」，点击复制到剪贴板
2. **文件复制** — 文件树右键菜单「复制」，在同目录创建 `_copy` 后缀副本
3. **`/` 命令弹窗** — 输入框首字符为 `/` 时弹出命令选择面板，支持过滤/键盘导航/快捷键展示，白名单限定 7 个核心命令
4. **slash 命令瘦身** — 移除 `session.share/unshare`、`terminal/mcp/agent/workspace` 的 slash 触发器，统一由新 slash 弹窗管理
5. **模型 context/output 上限显式化** — `settings-token-management` 为所有模型补充 `limit` 字段
6. **WebFetch 代理** — 新增 `OPENCODE_WEBFETCH_PROXY` 环境变量及工具参数 `proxy`，支持为 AI 网页抓取配置 HTTP 代理
7. **部署文档** — Docker 镜像版本更新，新增 `OPENCODE_WEBFETCH_PROXY` 环境变量示例

---

## 说明

- 上述内容为 **2026-04-14** 根据当前仓库工作区与 `HEAD` 的差异整理。
- `packages/sdk/js/src/v2/gen/` 下两个自动生成文件变更量最大，属 codegen 产物，不含手写逻辑。
- `File.share()` 中上传端点地址（`10.5.132.186:8000`）及 kudata-agent 域名为内网固定配置。
