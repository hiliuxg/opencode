# Git 变更回顾 - 2026-05-04

## 概览

分支：`codex/kudata-memory-20260504` 从 `dev` 当前 HEAD 切出，整理本次工作区未提交变更。

本次变更主要围绕 admin cron 手动执行结果返回、会话文件树侧栏，以及 UI 图标补充。

---

## 修改的文件详情

### Admin cron 执行链路

- `packages/admin/src/cron/executor.ts` 让 `enqueue` 在立即执行时返回 `ExecutionResult`；并发已满时仍进入队列并返回空结果。
- `packages/admin/src/cron/executor.ts` 将 opencode 调用从 `prompt_async` 改为 `/session/:id/message`，并读取完整响应文本，使任务耗时覆盖模型回复流完成时间。
- `packages/admin/src/server/routes/cron.ts` 手动触发任务时等待 `executor.enqueue`，立即完成则返回 `job completed` 和执行结果，进入队列则返回 `job queued`。
- `packages/admin/docs/api.md` 更新 `POST /api/jobs/:id/run` 文档，说明完成返回和排队返回两种响应。
- `packages/admin/src/cron/executor.test.ts` 新增测试，覆盖同步消息响应被完整读取，以及 `enqueue` 返回执行结果。

### App 会话侧栏与文件树

- `packages/app/src/pages/session.tsx` 将会话侧栏入口切换到 `session-side-panel-filetree`。
- `packages/app/src/pages/session/session-side-panel-filetree.tsx` 新增侧栏实现，保留 review/context/file tabs，并加入独立文件树区域。
- 新文件树侧栏支持根目录新建文件、新建文件夹、刷新、折叠全部、拖拽上传、文件/目录删除、重命名、移动、复制、预览、分享和下载等操作。
- 文件树根据 session diff 标记新增、删除或混合状态，并在打开文件 tab 时同步当前活动文件。
- `packages/app/src/pages/layout/conversation-sidebar.tsx` 微调新会话按钮间距，避免右侧布局显得过宽。

### UI 图标

- `packages/ui/src/components/icon.tsx` 新增 `file-plus` 和 `collapse-all` 图标，并重绘 `refresh` 图标为线框风格。
- `packages/ui/src/components/icon.stories.tsx` 将新增图标加入 story 列表，方便本地检查图标集。

---

## 本次变更的主题

1. **cron 手动执行可拿到真实结果**：立即执行的任务会等待 opencode 消息流完成，并把 session 和耗时结果返回给调用方。
2. **文件树成为会话侧栏的一等区域**：会话页面从旧侧栏切到包含文件树操作的新面板。
3. **文件树操作更完整**：补齐根目录创建、上传、折叠、复制、分享等常用文件操作。
4. **图标视觉补齐**：为新增文件树工具栏提供专用图标，并统一刷新按钮样式。

---

## 说明

- 上述内容根据 `2026-05-04` 工作区相对当前 `HEAD` 的差异整理。
- 本次记录包含 7 个已修改文件和 2 个新增代码文件，另新增本 `kudata-memory` 记录文件。

---

## 追加记录：文件树复制粘贴、分类新会话与路径提示

### 概览

分支：`codex/kudata-memory-20260504` 在上一轮提交后继续整理当前工作区变更。

本次追加变更主要围绕文件树剪切/复制/粘贴能力、服务端文件复制 API、会话分类下新建会话，以及文件 tab 路径提示。

### 修改的文件详情

#### 文件树剪切、复制与粘贴

- `packages/app/src/components/file-tree.tsx` 新增 `FileTreeClip` 状态、剪贴板路径解析、粘贴目标计算和剪切粘贴拦截逻辑。
- 文件和文件夹右键菜单新增复制、剪切和粘贴入口；剪切状态下对应节点半透明显示。
- `packages/app/src/pages/session/session-side-panel.tsx` 与 `session-side-panel-filetree.tsx` 接入共享剪切状态，支持在根目录或目录节点执行粘贴。
- 旧的前端文本读取复制逻辑切换为调用文件上下文的 `copy` 操作，目录复制也能走统一链路。
- `packages/app/src/i18n/en.ts`、`packages/app/src/i18n/zh.ts` 补齐复制、剪切、粘贴和剪贴板失败等文案。
- `packages/app/src/components/file-tree.test.ts` 覆盖剪贴板路径解析、粘贴目标和剪切目录到自身的拦截逻辑。

#### 服务端文件复制 API 与 SDK

- `packages/opencode/src/file/index.ts` 新增 `File.copy(src, dst)`，支持文件和目录递归复制。
- 复制目标已存在时自动生成 `copy`、`copy 2` 等后缀，避免覆盖原文件。
- 目录复制会拒绝把目录复制进自身内部，并继续使用项目目录边界检查。
- `packages/opencode/src/server/routes/file.ts` 新增 `POST /file/copy`，返回实际复制后的路径。
- `packages/opencode/test/file/index.test.ts` 覆盖文件不覆盖复制、目录递归复制和目录复制到自身的拒绝场景。
- `packages/sdk/js/src/v2/gen/sdk.gen.ts`、`types.gen.ts` 同步生成 `file.copy` 客户端方法和类型。

#### 会话分类与路径展示

- `packages/app/src/pages/layout.tsx` 支持打开新会话时携带 `catalog` 查询参数。
- `packages/app/src/pages/layout/conversation-sidebar.tsx` 在分类菜单中新增“新建会话”，并保持 archived 分类排在最后。
- `packages/app/src/pages/layout/helpers.ts` 新增 `catalogs` 排序 helper；`conversation-sidebar.test.ts` 补充 archived 分类排序测试。
- `packages/app/src/components/prompt-input/submit.ts` 新会话创建后会读取 `catalog` 查询参数，并调用 session update 绑定到对应分类。
- `packages/app/src/components/prompt-input/submit.test.ts` 覆盖从分类入口新建会话后写入 catalog 的流程。
- `packages/app/src/context/file/path.ts` 和 `session-sortable-tab.tsx` 新增文件 tab 绝对路径提示；`path.test.ts` 覆盖 Unix 与 Windows 路径。

### 本次追加变更的主题

1. **文件树操作更接近桌面文件管理器**：右键复制、剪切、粘贴可以跨目录工作，目录复制也走后端递归复制。
2. **复制行为更安全**：目标存在时自动改名，不覆盖已有文件；目录不能复制到自身内部。
3. **分类新会话流程闭环**：从分类菜单新建的会话会自动归入对应 catalog。
4. **文件 tab 信息更完整**：悬停 tab 可以看到当前工作区下的绝对路径。

### 说明

- 上述内容根据 `2026-05-04` 当前工作区相对 `HEAD` 的未提交差异整理。
- 本次追加记录覆盖 21 个代码/测试/生成文件的修改，以及本日志文件的更新。

---

## 追加记录：文件路径复制、标签页批量关闭与通知恢复

### 概览

分支：`codex/kudata-memory-20260504` 在 `feat: add file tree clipboard operations` 后继续整理当前工作区变更。

本次追加变更主要围绕文件树和文件 tab 的路径复制、文件 tab 右键批量关闭，以及只在当前工作区触发通知。

### 修改的文件详情

#### 文件路径复制

- `packages/app/src/utils/clipboard.ts` 新增 `copyPath`，统一封装剪贴板写入、成功提示和失败提示。
- `packages/app/src/utils/clipboard.test.ts` 覆盖成功写入路径和剪贴板不可用时的错误提示。
- `packages/app/src/components/file-tree.tsx` 在文件和目录右键菜单中新增“复制路径”，复制当前节点的绝对路径。
- `packages/app/src/components/session/session-sortable-tab.tsx` 复用 `copyPath`，让已打开文件 tab 也可以从右键菜单复制绝对路径。

#### 文件 tab 右键菜单

- `packages/app/src/components/session/session-sortable-tab.tsx` 新增右键菜单，支持关闭当前、全部关闭、关闭其他和复制路径。
- `packages/app/src/pages/session/helpers.ts` 新增 `closePlan`，集中计算批量关闭后的 tab 列表和激活 tab。
- `packages/app/src/pages/session/helpers.test.ts` 覆盖全部关闭时保留 `context` tab，以及关闭其他文件 tab 后激活目标 tab 的行为。
- `packages/app/src/pages/session/session-side-panel-filetree.tsx` 接入 `closePlan`，在批量关闭时通过 `batch` 同步更新 tab 列表和 active 状态。
- `packages/app/src/i18n/en.ts`、`zh.ts`、`zht.ts` 补齐“全部关闭”和“关闭其他”文案。

#### 当前工作区通知

- `packages/app/src/pages/layout/helpers.ts` 新增 `shouldNotify(active, dir)`，通过标准化 workspace key 判断通知是否属于当前工作区。
- `packages/app/src/pages/layout/helpers.test.ts` 覆盖 Unix、Windows 路径和空 active workspace 的通知判断。
- `packages/app/src/context/notification.tsx` 恢复 agent 响应和错误事件的声音、系统通知，但仅在事件目录属于当前工作区时触发。
- `packages/app/src/pages/layout.tsx` 恢复权限请求和问题事件的声音、系统通知、持久 toast，并在非当前工作区时提前跳过。

### 本次追加变更的主题

1. **复制路径入口更统一**：文件树节点和文件 tab 都可以复制绝对路径，并共用一套剪贴板提示逻辑。
2. **文件 tab 管理更完整**：右键菜单补齐关闭当前、全部关闭和关闭其他，批量关闭时保留固定的上下文 tab。
3. **通知恢复但更克制**：声音、系统通知和 toast 重新启用，同时避免非当前工作区事件打扰当前界面。

### 说明

- 上述内容根据 `2026-05-04` 当前工作区相对 `HEAD` 的未提交差异整理。
- 本次追加记录覆盖 12 个已跟踪代码/测试/文案文件的修改、2 个新增剪贴板工具文件，以及本日志文件的更新。
