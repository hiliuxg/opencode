# Git 变更回顾 - 2026-04-21

## 概览

分支：`dev`（相对当前 `HEAD` 的**工作区未提交变更**）  
修改文件总数：6 个已跟踪文件 + 2 个新增未跟踪 UI 组件文件  
已跟踪 diff 统计：约 +66 / -19 行；新增 `datatable.tsx` / `datatable.css` 约 129 行

---

## 修改的文件详情

### DEVELOPMENT.md

- 版本注释区：保留 `opencode-v1313-04142202` 行格式微调；**新增**一行 `# opencode-v1313-04171259`
- Docker 镜像标签与 tar 文件名：`opencode-v1313-04171259` → **`opencode-v1313-04202355`**（`buildx`、`docker save`、`docker load`、`docker run` 中镜像名一致更新）
- `docker run` 示例块末尾多余空行删减（纯格式）

---

### bun.lock

- **`ghostty-web`**：GitHub 引用从 `anomalyco/ghostty-web#4af877d` 更新为 **`#20bd361`**（resolved 包名与 integrity 同步变更）

---

### packages/app/src/pages/session/file-tabs.tsx

**Excel Tab 扩展名判断收窄**

- `isExcel` 正则由 `/\.(xlsx?|xlsm|xlsb|csv)$/i` 改为 **`/\.(xlsx?|xlsm|xlsb)$/i`**，即 **不再将 `.csv` 视为 Excel 预览分支**（与后端 spreadsheet 集合不含 `csv` 时行为更一致）

---

### packages/ui/src/components/datatable.tsx（新增，未跟踪）

**通用表格组件**

- `DataCol`：`key` / 可选 `label` / `title`（表头 `title` 用作 tooltip）
- `DataTable`：`cols` + `rows: unknown[][]`；可选 `empty`、自定义 `class`、`cell` 渲染器
- 默认单元格：`null`/`undefined` → `-`；标量转字符串；否则 `JSON.stringify`
- `text()`：复制用，折叠空白字符为单空格并 `trim`
- 右上角 **复制为 TSV**：表头 + 行，`navigator.clipboard`；`IconButton` + `Tooltip`；成功态 2s 内显示已复制（`ui.message.copy` / `ui.message.copied`）

---

### packages/ui/src/components/datatable.css（新增，未跟踪）

- `datatable-wrap`：`overflow: auto`、`position: relative`
- `datatable-copy`：`sticky` 顶对齐、右对齐、z-index
- `datatable`：全宽、`border-collapse`、小号等宽字体、`th`/`td` 边框与 padding；表头 `sticky` + `surface-base` 背景

---

### packages/ui/src/components/message-part.css

- 新增 **`[data-slot="mcp-sql-error"]`**：内边距、关键色文字、弱关键背景、圆角、13px、换行策略（与现有 MCP 无结果等 slot 风格体系一致）

---

### packages/ui/src/components/message-part.tsx

**MCP `run_query` 输出：JSON 视图 → 表格视图**

- 新增 import：`DataTable`（`./datatable`）
- 新增 **`SqlQueryRenderer`**：`props.data` 为 JSON 字符串 → `JSON.parse`；期望结构含 `success`、`message`、`data.result_msg`、`data.result.columns` / `data.result.rows`
- `columns`：过滤为带 `name` / `dataType` 的对象数组，映射为 `DataTable` 的 `cols`（`title` 用 `dataType`）
- `rows`：过滤为二维数组
- 错误：`success` 为假或解析失败时，fallback 为 **`data-slot="mcp-sql-error"`**，文案来自 `message` / `result_msg` / `"Unknown error"`；解析异常时附带错误信息
- 成功但无列：`data-slot="mcp-tool-no-results"` + 文案 **`No table schema`**（硬编码英文）
- 成功有列：`<DataTable ... empty="No rows" />`（`No rows` 硬编码英文）
- **`ToolRegistry` 中 MCP SQL 工具**（`run_query` 相关 Tabs）：`output` Tab 内原 `Dynamic` + `fileComponent` 渲染 `output.json` 改为 **`<SqlQueryRenderer data={output()} />`**（通过 `Show` 的 render prop 传入 `output`）

---

### packages/ui/src/styles/index.css

- 在 components 层增加：`@import "../components/datatable.css" layer(components);`

---

## 本次变更的主题

1. **MCP SQL 查询结果展示** — `run_query` 输出由原始 JSON 文件预览改为解析后 **`DataTable`** 表格；失败态统一 `mcp-sql-error` 样式
2. **可复用表格组件** — `@opencode-ai/ui` 新增 `DataTable` + 样式与全局 CSS 注册；支持 TSV 复制
3. **会话文件 Tab** — Excel 路径判断去掉 **`.csv`**，避免误走 Excel 二进制预览链
4. **依赖与运维记录** — `ghostty-web` 指到新 commit；`DEVELOPMENT.md` 中 Docker 镜像版本号更新为 `04202355`

---

## 说明

- 上述内容为 **2026-04-21** 根据当前仓库工作区与 `HEAD` 的差异整理。
- `datatable.tsx`、`datatable.css` 在 `git status` 中为 **Untracked**，已纳入本记忆文件描述。
- `SqlQueryRenderer` 内部分 fallback 文案（如 `No table schema`、`No rows`、`Unknown error`）为组件内硬编码英文；若需与 app 一致的双语策略，可后续改为 `useI18n` 键值。
