# Git 变更回顾 - 2026-04-09

## 概览

分支：`dev`（相对当前 `HEAD` 的**工作区未提交变更**）  
修改文件总数：11 个已跟踪文件 + 2 个新增工具文件  
总变更（已跟踪）：新增约 732 行，删除约 96 行

---

## 修改的文件详情

### DEVELOPMENT.md

- 版本注释行增加一条镜像记录：`45f94d09e8a3` ↔ `opencode-v125-03282001`
- Docker `run`：主机端口由 `4097:4096` 改为 `4096:4096`
- 新增环境变量示例：`-e OPENCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX=10240`
- 删除重复的 `/home/xiaogenliu/ccc.json` 占位行

---

### packages/app/src/components/settings-token-management.tsx

**TME 双 Provider 与模型拆分**

- 主 Provider：`tme-conv1-provider`（`@ai-sdk/github-copilot`，`name`: `tme-conv1`），默认模型仍为 `GPT-5.4`
- 第二 Provider：`tme-conv2-provider`（`@ai-sdk/openai-compatible`，`name`: `tme-conv2`），承载 DeepSeek / GLM / Kimi 等模型
- conv1 侧模型列表重组：突出 GPT / Gemini 系列，增加 `GPT-5-mini`、`GPT-5.1` 等条目；TME DeepSeek 保留在 conv1
- 写入令牌时：若存在 `tme-conv2-provider`，同步为其 `options.apiKey` 赋值（与 conv1 共用令牌）

---

### packages/app/src/i18n/en.ts / zh.ts

- **技能更新**：与 `skills.updates.*` 相关的检查/横幅/一键更新/落后 commit 文案（与技能页、侧边栏联动）
- **Dashboard**：`common.chart` / `common.table` / `common.date`；`dashboard.allDirs.*`、`dashboard.dailyDirs.*`、`dashboard.dailyDirDetail.*`
- **数据分析技能**：`skills.kb.creator.prompt` 英文版与中文版均大幅扩写（结构化步骤、表结构、指标、FAQ、分区与 SQL 约束等）

---

### packages/app/src/pages/dashboard.tsx

**统计维度与图表/表格双视图**

- `DayStats` 增加 `dirs: Record<string, true>`，按日聚合「当日出现过的目录」
- 会话/消息拉取 `limit` 由 2000 提到 3000
- 新增 `ChartWithTable`：图表与表格 Tab 切换（`Button` + `useLanguage`）
- 每日 Token、每日问题/会话图改为 `ChartWithTable`，日期列走 i18n
- 新增「每日活跃目录数」折线图 + 表格（`dailyDirCounts`、`dailyDirChartOption`）
- `topSessions` / `topDirs` / `topProviderModels` 展示数量 Top 10 → Top 20；Provider/Model 副标题同步
- 新增「所有目录」大表（按 Token 排序，可滚动，目录链到 workspace）
- 新增「每日目录明细」表：`dailyDirRows`（日期升序，每日下列出当日活跃目录）
- 部分区块标题仍为中文硬编码（如「每日 Token 消耗」），新板块已用 `language.t`

---

### packages/app/src/pages/layout.tsx

- 引入 `gitAccount`、`setSkillUpdates`、`skillUpdateCount`
- 新增 `useSkillUpdatePolling()`：挂载后随 `currentDir()` 变化，每 5 分钟请求 `GET /skill/check-updates?directory=&name=`，静默忽略网络错误
- 向 `SidebarContent` 传入 `skillsUpdatesLabel`：`skills.updates.tooltip` + `skillUpdateCount()`

---

### packages/app/src/pages/layout/sidebar-shell.tsx

- `SidebarContent` 新增 prop：`skillsUpdatesLabel`
- 技能入口：`Tooltip` 内双行（技能标题 + 有更新时的红色副文案）；知识库按钮右上角脉冲红点（`hasSkillUpdates()`）
- 引入 `hasSkillUpdates`、`skillUpdateCount`

---

### packages/app/src/pages/skills.tsx

- 本地 `accountFromWorkspaceDir` 删除，统一使用 `gitAccount(dir)`
- 引入 `skillUpdates` / `setSkillUpdates`；拉取成功后从更新 map 中移除该技能
- 新增 `pullSkillSilent`、`handleCheckUpdates`、`handleUpdateAll`；顶部横幅 +「检查更新」主按钮；移除原「刷新」按钮；「创建技能」按钮降为 secondary
- 卡片上展示落后分支与 commit 数；有更新时下载按钮高亮

---

### packages/opencode/src/server/server.ts

- 根级 query validator 增加可选 `name`（与部分路由查询参数兼容）
- 引入 `node:path`
- 新增 `GET /skill/check-updates`：按技能 Git 仓库去重、`git fetch`、可选 HTTP 远程注入 `OPENCODE_SKILL_MARKET_TOKEN`、计算 `HEAD..origin/<defaultBranch>` 落后数，返回 `{ updates: Record<skillName, { behind, branch }> }`

---

### packages/opencode/src/session/llm.ts

- `maxOutputTokens`：不再对 GitHub Copilot / OpenAI OAuth 单独置 `undefined`，统一走 `ProviderTransform.maxOutputTokens(input.model)`（便于配合 `OPENCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX` 等全局上限）
- 日志增加 `providerID`
- 非 fetch 场景下 `User-Agent`：除原逻辑外，对原排除的 provider 也统一带上 `opencode/${VERSION}`（缩进与分支整理）

---

### packages/opencode/src/session/prompt.ts

- 模型 `finish` 且尚无 `error` 时：检查是否存在有效 `text`/`reasoning` 内容；若无且 `finish !== "stop"`，置 `Unknown` 错误（中文提示令牌/切换模型/令牌管理），写回消息并中断流

---

## 新增文件

### packages/app/src/utils/git-account.ts

- 从工作区路径解析技能同步用账号：`/home/{user}/...` 取段 2；`/Users/leoliu/myroom...` 映射固定账号 `xiaogenliu`

### packages/app/src/utils/skill-updates.ts

- `skillUpdates` / `setSkillUpdates` 全局信号；`hasSkillUpdates`、`skillUpdateCount` 辅助方法

---

## 本次变更的主题

1. **技能更新闭环** — 后端 `check-updates`、布局轮询、侧边栏红点、技能页检查/一键更新与状态同步  
2. **TME 双 Provider 配置** — Copilot 与 OpenAI-Compatible 分流，令牌一键写入两处  
3. **Dashboard 增强** — 目录维度统计、图表/表格切换、全量目录与按日明细、Top 20 与更高抓取上限  
4. **LLM 输出上限** — Copilot 等也参与统一 `maxOutputTokens` 解析，便于环境变量调输出长度  
5. **空响应容错** — 无正文完成时提示额度/令牌问题  
6. **部署文档** — Docker 端口与 `OPENCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX` 示例  

---

## 说明

- 上述内容为 **2026-04-09** 根据当前仓库工作区与 `HEAD` 的差异整理；若之后分批提交，可按 commit 再拆记忆条目。
- 未纳入：`kudata-memory` 历史文件、`*.tar` 等大文件仅作本地产物，非功能代码变更。
